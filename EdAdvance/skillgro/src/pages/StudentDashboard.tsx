import Wrapper from '../layouts/Wrapper';
import SEO from '../components/SEO';

const StudentDashboard = () => {
   return (
      <Wrapper>
         <SEO pageTitle={'EdAdvance Student Dashboard'} />
    
      </Wrapper>
   );
};

export default StudentDashboard;